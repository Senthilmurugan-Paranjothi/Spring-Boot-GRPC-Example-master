# Spring-Boot-GRPC-Example-master
A minimal, clean Spring Boot + gRPC example demonstrating how to expose a `getUser` RPC endpoint, map domain objects to protobuf messages, and run a lightweight gRPC server.   Spring-Boot-GRPC-Example-masterThis project is intentionally small — only six core files — making it ideal for learning, demos, or onboarding new developers.
